function add(n1: number, n2: number, showResult: boolean) {
  if (showResult) {
    return n1 + n2;
  }
}

const number1 = 4;
const number2 = 5;
const result = true;

console.log(add(number1, number2, result));
